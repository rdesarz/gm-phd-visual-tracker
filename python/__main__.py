def main():
    # In a loop:
    # Load image and its associated timestamp
    # image, timestamp = load_image()

    # Load bounding box and generate fake detections
    # bounding_box = read_bounding_box()
    # detections = generate_detections(image, bounding_box)

    # Iterate over each timestamps and input them into the tracker
    # tracker.processDetections(timestamp, detections)

    # Save result
    # recorder.add(tracker.getTrack())

    # Display result

    pass


if __name__ == '__main__':
    main()
